# Okta OAuth 2.0 Configuration
# Required for Okta authentication integration

# Okta Domain (required)
# Your Okta organization domain (without https://)
# Example: dev-12345678.okta.com or your-company.okta.com
OKTA_DOMAIN=your-tenant.okta.com

# Okta Client ID (required)
# OAuth 2.0 client ID from your Okta application
# Found in: Applications > [Your App] > General > Client Credentials
OKTA_CLIENT_ID=your_client_id_here

# Okta Client Secret (required)
# OAuth 2.0 client secret from your Okta application
# Found in: Applications > [Your App] > General > Client Credentials
# IMPORTANT: Keep this secret secure and never commit to version control
OKTA_CLIENT_SECRET=your_client_secret_here

# Authorization Server (optional)
# Options: 'default', 'org', or custom authorization server ID
# - 'default': Uses default custom authorization server (/oauth2/default)
# - 'org': Uses org authorization server (no /oauth2/default path)
# - Custom ID: Uses specific custom authorization server (/oauth2/{id})
# Default: 'default'
OKTA_AUTH_SERVER=default

# Redirect URI (optional)
# OAuth 2.0 callback URL after authentication
# Must match the redirect URI configured in your Okta application
# Default: {APP_URL}/api/auth/okta/callback
# OKTA_REDIRECT_URI=http://localhost:3000/api/auth/okta/callback

# Okta API Token (optional)
# Required only for advanced features:
# - User registration and management
# - Okta session management
# - Custom user attributes
# Found in: Security > API > Tokens
# OKTA_API_TOKEN=your_okta_api_token_here

# Application Base URL (required)
# Base URL of your application, used to compute redirect URI
# Development: http://localhost:3000
# Production: https://your-domain.com
APP_URL=http://localhost:3000
